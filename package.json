{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:e2e": "npx playwright test --project='chromium UI tests'",
    "test:api": "npx playwright test --project=\"API tests\"",
    "test:ui": "npx playwright test --project=\"UI tests\"",
    "test:ui2": "npx playwright test --ui --project=chromium",
    "test:all": "npx playwright test"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.11.30"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "moment": "^2.30.1"
  }
}
